---
interface Props {
  id?: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="py-6">
  <h2 class="text-2xl font-bold mb-4">{title}</h2>
  <slot />
</section>
